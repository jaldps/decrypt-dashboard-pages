# Decrypt analytics dashboard — encrypted artifact

This repo holds a single AES-256-encrypted HTML file. The dashboard
is served via GitHub Pages and only renders for someone with the
password. Source code lives in a separate private repository.

Snapshot rebuilt automatically every 30 minutes.
